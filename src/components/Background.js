const Background = () => {
  return (
    <div className="bg-gray-900 fixed bottom-0 left-0 w-full h-[50vh] z-[-1]">
      {/* This will fill the bottom 50% of the screen */}
    </div>
  );
};

export default Background;
